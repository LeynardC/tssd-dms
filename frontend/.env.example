# Origin of the Laravel API. No trailing slash.
VITE_API_BASE_URL=http://localhost:8000

# "true" shows the "Sign in with Google" button and the Link Google Account
# control in Settings. Leave unset/false unless GOOGLE_CLIENT_ID etc. are
# configured on the backend, or the buttons lead to a broken redirect.
VITE_GOOGLE_ENABLED=false
